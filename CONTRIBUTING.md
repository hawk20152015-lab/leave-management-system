# 🤝 دليل المساهمة

شكراً لاهتمامك بالمساهمة في هذا المشروع! 🎉

## 📋 جدول المحتويات

- [كيفية البدء](#كيفية-البدء)
- [تقديم الأخطاء](#تقديم-الأخطاء)
- [طلب ميزة جديدة](#طلب-ميزة-جديدة)
- [الكود النظيف](#الكود-النظيف)
- [عملية Pull Request](#عملية-pull-request)

---

## 🚀 كيفية البدء

### 1. Fork المشروع
```bash
# على الموقع، اضغط "Fork"
```

### 2. Clone الـ Fork
```bash
git clone https://github.com/USERNAME/leave-management-system.git
cd leave-management-system
```

### 3. أنشئ branch جديد
```bash
git checkout -b feature/your-feature-name
```

### 4. اعمل على الميزة
```bash
# اكتب الكود
# اختبره
```

### 5. Commit التعديلات
```bash
git add .
git commit -m "وصف واضح للميزة"
```

### 6. Push للـ Fork
```bash
git push origin feature/your-feature-name
```

### 7. افتح Pull Request
- على GitHub
- اختر الـ branch الخاص بك
- اكتب وصف تفصيلي

---

## 🐛 تقديم الأخطاء

### قبل الإبلاغ:
1. ✅ تحقق من المشروع الحالي
2. ✅ ابحث عن أخطاء مشابهة
3. ✅ جرب على Python 3.8+

### عند الإبلاغ:
1. اضغط "Issues" → "New issue"
2. اختر "Bug report"
3. املأ:
   - **العنوان**: وصف قصير للخطأ
   - **الوصف**: 
     - ما الذي حدث؟
     - ما المتوقع؟
     - الخطوات لتكرار الخطأ
   - **البيئة**:
     - إصدار Python
     - نظام التشغيل
     - المتصفح (إن كان)

### مثال:
```
العنوان: خطأ عند إضافة إجازة طويلة

الوصف:
عند محاولة إضافة إجازة أطول من 30 يوم، يظهر خطأ.

الخطوات:
1. انقر "إضافة إجازة"
2. اختر موظف
3. أدخل تاريخ قديم وحديث (أكثر من 30 يوم)
4. اضغط حفظ

الخطأ: "ValueError: أيام الإجازة غير صحيحة"

البيئة:
- Python 3.9
- Windows 10
- Chrome
```

---

## ✨ طلب ميزة جديدة

### عند اقتراح ميزة:
1. اضغط "Issues" → "New issue"
2. اختر "Feature request"
3. املأ:
   - **الوصف**: ما الميزة المطلوبة؟
   - **المشكلة**: لماذا نحتاجها؟
   - **الحل المقترح**: كيف ستحل المشكلة؟
   - **بدائل**: هل هناك حلول أخرى؟

### مثال:
```
العنوان: إضافة تصدير PDF للإجازات

الوصف:
نريد إمكانية تصدير قائمة الإجازات إلى PDF

المشكلة:
الآن يمكن فقط رؤية الإجازات في الويب، لا يمكن حفظها

الحل المقترح:
إضافة زر "تصدير PDF" في تبويب الإجازات

البدائل:
- تصدير Excel (بدلاً من PDF)
- طباعة مباشرة من المتصفح
```

---

## 💻 الكود النظيف

### معايير الكود:

#### 1. التسمية واضحة
```python
# ❌ خطأ
def fn(e):
    return e['n']

# ✅ صحيح
def get_employee_name(employee):
    return employee['name']
```

#### 2. Comments مفيدة
```python
# ❌ خطأ
x = 30  # الرقم 30

# ✅ صحيح
annual_leave_days = 30  # عدد أيام الإجازة السنوية
```

#### 3. دوال صغيرة
```python
# ❌ خطأ - دالة ضخمة
def process_everything():
    # 200 سطر من الكود

# ✅ صحيح - دوال صغيرة
def get_employees():
    pass

def filter_active_employees(employees):
    pass

def sort_by_name(employees):
    pass
```

#### 4. عدم تكرار الكود
```python
# ❌ خطأ
def add_employee():
    conn = sqlite3.connect('db.db')
    # ...
    conn.close()

def update_employee():
    conn = sqlite3.connect('db.db')
    # ...
    conn.close()

# ✅ صحيح
def get_connection():
    return sqlite3.connect('db.db')

def add_employee():
    conn = get_connection()
    # ...
    conn.close()
```

#### 5. معالجة الأخطاء
```python
# ❌ خطأ
def save_data(data):
    conn.execute(...)  # قد تفشل!

# ✅ صحيح
def save_data(data):
    try:
        conn.execute(...)
    except sqlite3.Error as e:
        print(f"❌ خطأ: {str(e)}")
        return False
    return True
```

---

## 📝 عملية Pull Request

### قبل فتح PR:
1. ✅ اختبر الكود محلياً
2. ✅ تأكد من عدم وجود أخطاء
3. ✅ أضف تعليقات إذا لزم
4. ✅ اتبع معايير الكود

### رسالة PR جيدة:
```markdown
## وصف
وصف قصير للتغييرات

## نوع التغيير
- [ ] إصلاح خطأ
- [ ] ميزة جديدة
- [ ] تحسين الأداء
- [ ] توثيق

## التغييرات:
- تم إضافة دالة لحساب الإجازات
- تم إصلاح خطأ في التاريخ
- تم تحديث الواجهة

## كيفية الاختبار:
1. افتح التطبيق
2. اذهب لـ "إضافة إجازة"
3. أدخل البيانات
4. يجب أن تظهر رسالة نجاح

## Screenshots (إن أمكن):
[صورة قبل]
[صورة بعد]

## Checklist:
- [x] اختبرت الكود محلياً
- [x] لا توجد أخطاء جديدة
- [x] أتبعت معايير الكود
```

---

## 🎯 قواعس ذهبية

✅ **اختبر قبل الإرسال**
✅ **اكتب رسائل commit واضحة**
✅ **اتبع معايير الكود**
✅ **كن احترافياً ومهذباً**
✅ **استمع للملاحظات**

❌ **لا ترسل تعديلات عشوائية**
❌ **لا تغير أكثر من ميزة واحدة**
❌ **لا تستخدم لغة غير احترافية**
❌ **لا تنسخ الكود الآخرين**

---

## 📞 الدعم

هل تحتاج مساعدة؟
- افتح Issue
- اسأل في Discussions
- أرسل بريد

---

## 📜 الترخيص

بالمساهمة، أنت توافق على ترخيص المشروع تحت MIT.

---

**شكراً لمساهمتك! 🎉**

أي أسئلة؟ فتح Issue!
