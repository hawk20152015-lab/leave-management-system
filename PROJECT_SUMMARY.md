# 📋 تطبيق رصيد الاجازات والدوام - ملخص شامل

## 🎯 نظرة عامة

تطبيق متقدم **متعدد الميزات** لإدارة رصيد الإجازات والدوام والخفر للموظفين في المؤسسات العراقية.

التطبيق مبني بـ **Kivy/KivyMD** (Python) بواجهة عربية كاملة وقاعدة بيانات SQLite متقدمة.

---

## ✨ المميزات الأساسية

### 1️⃣ إدارة الموظفين
- ✅ إضافة/تعديل/حذف الموظفين
- ✅ تحديد نوع الدوام (صباحي/مسائي)
- ✅ تحديد البديل (المعاكس) لكل موظف
- ✅ تتبع حالة الموظف (نشط/غير نشط/في إجازة)

### 2️⃣ نظام حساب الدوام الذكي

#### 📅 الدوام الصباحي (يومي):
```
أيام العمل = أيام السنة - الجمعات والسبتات - الأعطال الرسمية
```
- ✅ يعمل كل يوم ما عدا الجمعة والسبت
- ✅ استثناء الأعطال الرسمية العراقية
- ✅ مشمول بالأعطال (لا يُحسب من الرصيد)
- ✅ يأخذ 30 يوم إجازة سنوية

#### 🌙 الخفر (المسائي):
```
أيام الخفر = أيام السنة - الجمعات والسبتات
```
- ✅ يعمل طول السنة ما عدا الجمعة والسبت فقط
- ✅ **لا** يتم استثناء الأعطال الرسمية
- ✅ **غير مشمول** بالأعطال (يعمل فيها)
- ❌ لا يأخذ إجازات سنوية

### 3️⃣ نظام الوجبات والبدلاء
- ✅ كل موظف خفر له بديل (موظف صباحي معاكس)
- ✅ البديل يعكس الوجبة (صباحي ↔ مسائي)
- ✅ تتبع تفصيلي لكل وجبة

### 4️⃣ حساب الرصيد السنوي
- ✅ رصيد أساسي 30 يوم للدوام الصباحي
- ✅ حساب تلقائي للإجازات المأخوذة
- ✅ عرض الرصيد المتبقي
- ✅ تقارير تفصيلية للسنة بأكملها

### 5️⃣ الأعطال الرسمية المدمجة
التطبيق يحتوي على **9 أعطال رسمية** عراقية:
1. رأس السنة الميلادية (1 يناير)
2. يوم الشهيد (1 فبراير)
3. عيد الفلاح (11 فبراير)
4. نوروز (20 مارس)
5. عيد الحب (9 أبريل)
6. عيد العمال (10 أبريل)
7. عيد الأم (1 مايو)
8. ذكرى ثورة 14 تموز (17 يوليو)
9. عيد الاستقلال (15 أغسطس)

---

## 📁 هيكل المشروع

```
leave_management_app/
│
├── 📱 leave_management_app.py         ⭐ التطبيق الرئيسي
│   ├─ LeaveDatabase: إدارة قاعدة البيانات
│   ├─ LeaveCalculator: حسابات الدوام والرصيد
│   ├─ EmployeeAddPopup: نافذة إضافة الموظفين
│   └─ LeaveManagementApp: الواجهة الرسومية الرئيسية
│
├── 🔧 utils_and_reports.py           دوال مساعدة وتقارير
│   ├─ ReportGenerator: توليد التقارير
│   ├─ DataExporter: تصدير البيانات (JSON/CSV/HTML)
│   ├─ DataImporter: استيراد البيانات
│   ├─ BackupManager: النسخ الاحتياطية
│   └─ Validator: التحقق من صحة البيانات
│
├── 📊 sample_data.py                 بيانات تجريبية للاختبار
│   ├─ populate_sample_data(): إضافة موظفين وإجازات
│   └─ print_statistics(): عرض الإحصائيات
│
├── 🎛️ main.py                        القائمة الرئيسية التفاعلية
│   ├─ تشغيل التطبيق الرسومي
│   ├─ إضافة بيانات تجريبية
│   ├─ عرض الإحصائيات
│   ├─ تصدير البيانات
│   └─ النسخ الاحتياطي
│
├── 📋 requirements.txt                المتطلبات والمكتبات
│   ├─ kivy>=2.1.0
│   ├─ kivymd>=1.1.1
│   ├─ matplotlib>=3.5.0
│   └─ python-dateutil>=2.8.0
│
├── 🏗️ buildozer.spec                 إعدادات بناء APK للأندرويد
│
├── 📚 README.md                      الدليل الكامل (شامل)
├── ⚡ QUICK_START.md                 دليل البدء السريع
├── 📖 PROJECT_SUMMARY.md             هذا الملف
│
└── 📦 leave_management.db            قاعدة البيانات (تُنشأ تلقائياً)
```

---

## 🗄️ قاعدة البيانات (SQLite)

### 1. جدول الموظفين (employees)
```sql
CREATE TABLE employees (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    position TEXT,
    hire_date TEXT,
    shift_type TEXT,              -- 'صباحي' or 'مسائي'
    backup_employee_id INTEGER,   -- معرف الموظف البديل
    status TEXT DEFAULT 'نشط',
    annual_leave_balance REAL DEFAULT 30,
    sick_leave_balance REAL DEFAULT 10,
    unpaid_leave_balance REAL DEFAULT 0,
    created_at TIMESTAMP
);
```

### 2. جدول أيام الدوام (shift_days)
```sql
CREATE TABLE shift_days (
    id INTEGER PRIMARY KEY,
    date TEXT NOT NULL,
    employee_id INTEGER NOT NULL,
    shift_type TEXT,
    is_backup INTEGER DEFAULT 0,
    is_holiday INTEGER DEFAULT 0,
    notes TEXT
);
```

### 3. جدول طلبات الإجازات (leave_requests)
```sql
CREATE TABLE leave_requests (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    leave_type TEXT,              -- 'إجازة سنوية', 'مرضية', 'بدون راتب'
    start_date TEXT NOT NULL,
    end_date TEXT NOT NULL,
    days REAL,
    status TEXT DEFAULT 'معلق',   -- 'معلق', 'موافق عليه', 'مرفوض'
    notes TEXT,
    created_at TIMESTAMP
);
```

### 4. جدول الأعطال الرسمية (holidays)
```sql
CREATE TABLE holidays (
    id INTEGER PRIMARY KEY,
    date TEXT NOT NULL UNIQUE,
    holiday_name TEXT,
    holiday_type TEXT DEFAULT 'رسمي'
);
```

---

## 🖥️ الواجهات الرسومية

### 📱 التبويبات الرئيسية:

1. **تبويب الموظفين**
   - قائمة الموظفين مع البيانات الأساسية
   - زر إضافة موظف جديد
   - حذف/تعديل البيانات

2. **تبويب رصيد الإجازات**
   - جدول شامل للرصيد
   - عرض حسب السنة
   - أعمدة: الاسم، نوع الدوام، الرصيد الأساسي، أيام مأخوذة، متبقي

3. **تبويب أيام الدوام**
   - إحصائيات الدوام الصباحي والخفر
   - اختيار الموظف والسنة
   - عرض تفصيلي لأيام العمل

4. **تبويب الإجازات**
   - إضافة طلب إجازة
   - قائمة الطلبات الموافق عليها والمعلقة
   - حالة الطلب والتاريخ

---

## 🚀 كيفية الاستخدام

### الطريقة 1️⃣: التطبيق الرسومي المباشر
```bash
python3 leave_management_app.py
```

### الطريقة 2️⃣: القائمة الرئيسية التفاعلية
```bash
python3 main.py
```

### الطريقة 3️⃣: بعد الإعداد الأولي
```bash
bash setup.sh      # تثبيت المتطلبات
python3 main.py    # تشغيل القائمة
```

---

## 📊 الحسابات والصيغ

### حساب أيام الدوام الصباحي:
```
total_working_days = 0

for day in year:
    if day.weekday not in [Friday, Saturday]:        # ليس نهاية أسبوع
        if not is_holiday(day):                      # ليست عطلة رسمية
            total_working_days += 1

result = total_working_days
```

### حساب أيام الخفر:
```
total_shift_days = 0

for day in year:
    if day.weekday not in [Friday, Saturday]:        # ليس نهاية أسبوع
        # الأعطال الرسمية لا تُستثنى!
        total_shift_days += 1

result = total_shift_days
```

### حساب رصيد الإجازات:
```
if employee.shift_type == 'صباحي':
    initial_balance = 30
else:
    initial_balance = 0

taken_days = sum(approved_leave_requests)
remaining = max(initial_balance - taken_days, 0)
```

---

## 🔄 سير العمل

### إضافة موظف جديد:
1. النقر على "إضافة موظف جديد"
2. ملء النموذج:
   - الاسم: "أحمد محمد"
   - المنصب: "مهندس برمجيات"
   - تاريخ التعيين: "2024-01-15"
   - نوع الدوام: "صباحي"
3. النقر على "إضافة"

### طلب إجازة:
1. الذهاب إلى تبويب "الإجازات"
2. النقر على "طلب إجازة جديدة"
3. اختيار:
   - نوع الإجازة
   - تاريخ البداية والنهاية
   - عدد الأيام
4. إرسال الطلب

### عرض الرصيد:
1. الذهاب إلى "رصيد الإجازات"
2. اختيار السنة
3. النقر على "تحديث"

---

## ⚙️ التثبيت والمتطلبات

### المتطلبات النظامية:
- Python 3.8 أو أحدث
- 100 MB مساحة فارغة
- الاتصال بالإنترنت (للتثبيت الأول)

### المكتبات المطلوبة:
```
kivy>=2.1.0
kivymd>=1.1.1
matplotlib>=3.5.0
python-dateutil>=2.8.0
```

### خطوات التثبيت:
```bash
# 1. نسخ المشروع
cd leave_management_app

# 2. تثبيت المتطلبات
pip install -r requirements.txt

# 3. (اختياري) إضافة بيانات تجريبية
python3 sample_data.py

# 4. تشغيل التطبيق
python3 leave_management_app.py
```

---

## 📤 التصدير والنسخ الاحتياطي

### تصدير البيانات:
- **JSON**: للمعالجة الآلية
- **HTML**: لعرض التقارير
- **CSV**: للإدراج في Excel

### النسخ الاحتياطية:
```bash
# من main.py اختر الخيار 5
# يتم حفظ النسخة في مجلد: backups/
```

---

## 🎯 حالات الاستخدام

### 1. مؤسسة حكومية عراقية
- ✅ إدارة موظفي الدوام الصباحي (صباح)
- ✅ إدارة موظفي الخفر (ليل)
- ✅ حساب رصيد الإجازات السنوي
- ✅ احترام الأعطال الرسمية العراقية

### 2. الشركات الخاصة
- ✅ تتبع أيام العمل الفعلية
- ✅ إدارة نوبات العمل
- ✅ حساب الموظفين المتاحين

### 3. المستشفيات والخدمات الطبية
- ✅ إدارة الخفر والدوام
- ✅ جدولة الطاقم الطبي
- ✅ حساب ساعات العمل

---

## 🔐 الأمان والخصوصية

- ✅ قاعدة بيانات محلية (SQLite)
- ✅ بدون اتصال إنترنت مستمر
- ✅ نسخ احتياطية تلقائية
- ✅ التحقق من صحة البيانات

---

## 🚨 ملاحظات مهمة ⚠️

1. **البيانات محفوظة محلياً**: يجب عمل نسخ احتياطية دورية
2. **الأعطال الرسمية**: مدرجة للعراق 2024-2026 فقط
3. **الواجهة العربية**: مدعومة بالكامل
4. **الأداء**: مُحسّن لقواعد بيانات تصل لآلاف الموظفين

---

## 📞 الدعم والمساعدة

### الملفات المرجعية:
1. **README.md**: دليل شامل مفصل
2. **QUICK_START.md**: بدء سريع بـ 5 دقائق
3. **هذا الملف**: ملخص الميزات والهيكل

### في حالة المشاكل:
1. تحقق من المتطلبات
2. اقرأ ملف README.md
3. جرب البيانات التجريبية

---

## 🎉 الإصدارات والتحديثات

**الإصدار الحالي**: 1.0.0  
**تاريخ الإصدار**: أغسطس 2024  

### المخطط المستقبلي:
- [ ] واجهة ويب (Web)
- [ ] تقارير PDF متقدمة
- [ ] مزامنة السحابة
- [ ] إشعارات تنبيهية
- [ ] دعم لغات إضافية

---

## 📝 الملاحظات التقنية

### معمارية التطبيق:
```
┌─────────────────────────────────────┐
│    Kivy/KivyMD (الواجهة الرسومية)    │
├─────────────────────────────────────┤
│     LeaveManagementApp (منطق)       │
├─────────────────────────────────────┤
│   LeaveDatabase + LeaveCalculator    │
├─────────────────────────────────────┤
│   SQLite (قاعدة البيانات)             │
└─────────────────────────────────────┘
```

### اللغات والإطارات:
- **لغة البرمجة**: Python 3
- **الواجهة الرسومية**: Kivy + KivyMD
- **قاعدة البيانات**: SQLite
- **التقارير**: Matplotlib
- **نظام التشغيل**: Windows, macOS, Linux, Android

---

## ✅ قائمة التحقق قبل الاستخدام

- [ ] Python 3.8+ مثبت
- [ ] المتطلبات مثبتة (`pip install -r requirements.txt`)
- [ ] لا توجد أخطاء في التشغيل
- [ ] البيانات التجريبية تم إضافتها (اختياري)
- [ ] النسخة الاحتياطية الأولى تم إنشاؤها

---

## 🙏 شكر خاص

تم تطوير هذا التطبيق بناءً على متطلبات:
- إدارة الموظفين في المؤسسات العراقية
- حساب الدوام والخفر بدقة
- احترام الأعطال الرسمية العراقية
- واجهة عربية سهلة الاستخدام

---

<div align="center">

### 🛠️ تطوير وتصميم بواسطة **عمر كريم**

**جميع الحقوق محفوظة © 2024**

</div>
